# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
shap>=0.42.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Environment
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
httpx>=0.24.0

# Utilities
joblib>=1.3.0

# Frontend (Stage 8 — Streamlit dashboard)
streamlit>=1.31.0
plotly>=5.18.0
requests>=2.31.0

# ---------------------------------------------------------------------------
# Stage 9 — GenAI Copilot: OPTIONAL LLM SDKs (NOT required for core pipeline)
# ---------------------------------------------------------------------------
# The /copilot endpoint degrades gracefully to a deterministic template fallback
# when no LLM key is configured. Install the SDK for whichever provider you use.
#
# For OpenAI (LLM_PROVIDER=openai):
#   pip install openai>=1.0.0
#
# For Google Gemini (LLM_PROVIDER=gemini):
#   pip install google-generativeai>=0.7.0
#
# Uncomment the line(s) below only if you want to install them as project deps:
# openai>=1.0.0
# google-generativeai>=0.7.0

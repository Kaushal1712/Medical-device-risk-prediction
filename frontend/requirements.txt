# frontend/requirements.txt
# Standalone requirements for the Streamlit dashboard only.
# (The root requirements.txt also includes these for single-environment setups.)
streamlit>=1.31.0
plotly>=5.18.0
requests>=2.31.0
python-dotenv>=1.0.0
